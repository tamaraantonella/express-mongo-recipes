version: '3.9'

services:
  mongodb:
    image: mongo:5.0
    container_name: mongo-db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=mongouser
      - MONGO_INITDB_ROOT_PASSWORD=secret123
    ports:
      - 27017:27017